version: "3.5"
services:
  near:
    image: near.near.dnp.dappnode.eth:0.1.0
    build:
      context: near
      args:
        UPSTREAM_VERSION: 2.3.1
    volumes:
      - near:/srv/near
    restart: unless-stopped
    ports:
      - 24567:24567/tcp
      - 24567:24567/udp
volumes:
  near: {}
